version: "2"

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - misspell
    - bodyclose
  exclusions:
    rules:
      # Test files don't need to check every error return.
      - path: "_test\\.go"
        linters: [errcheck]

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: true
